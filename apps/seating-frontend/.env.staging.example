# Staging build environment — used when Vite builds the frontend for the staging
# deploy. Baked at build time; never contains secrets. See plan TASK-003.
VITE_SEATING_API_URL=https://classprints-api.<STAGING_SUBDOMAIN>.workers.dev
